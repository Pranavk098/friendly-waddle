# shared

KB loader, LLM client, injection guard, rate limiter — shared by `interview-agent` and `mcp-server`.

Scaffolded in Phase 0. Implementation begins in Phase 1 (rate limiter, KB loader) and Phase 2 (injection guard), per `../docs/sdlc-plan.md`.
