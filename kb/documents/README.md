# kb/documents/

Long-form markdown deep-dives loaded on demand by both surfaces: `resume.md`, one `projects/<slug>.md` per major project, `research/<slug>.md`, `faq.md` (see spec §1.3).

Scaffolded in Phase 0; content and the loader that serves these on demand are added starting Phase 1, per `../../docs/sdlc-plan.md`.
