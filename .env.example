# Copy to .env and fill in real values. Never commit .env.

# --- Shared ---
ANTHROPIC_API_KEY=
MODEL_TIER=default          # default | economy (economy falls back to Haiku)

# --- interview-agent ---
DAILY_SPEND_CAP_USD=5
CORS_ALLOWED_ORIGIN=https://pranavkoduru.dev
SESSION_DB_PATH=./interview-agent/data/sessions.db

# --- mcp-server ---
MCP_RATE_LIMIT_PER_MIN=30
MCP_RATE_LIMIT_PER_DAY=300

# --- ops ---
BETTERSTACK_HEARTBEAT_URL=
DIGEST_EMAIL_TO=
