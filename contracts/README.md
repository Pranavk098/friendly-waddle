# contracts

pytest-based behavioral contract suite ("agentspec") that hits the real deployed prompt + KB + model: no-fabrication, gap honesty, injection resistance, scope deflection, cross-surface consistency.

Scaffolded in Phase 0. First contracts land in Phase 2 (`check_fit` injection resistance); full ~20-30 contract suite and public `/contracts/status` panel in Phase 5, per `../docs/sdlc-plan.md`.
