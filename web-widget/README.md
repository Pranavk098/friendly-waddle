# web-widget

Preact chat + contracts-status widget embedded as an island in the existing Astro portfolio site (pranavkoduru.dev). Not a Python package — no `uv` workspace membership; scaffolded with its own Node tooling when built.

Scaffolded in Phase 0. Implementation begins in Phase 4, per `../docs/sdlc-plan.md`.
